#
# Script para fazer build da imagem e iniciar o processo no container
#

# Faz build da imagem para garantir que as gems estao instaladas
docker build -t ruby-whois .

# Comando docker run conforme https://hub.docker.com/r/erikap/ruby-sinatra/
docker run \
	--name domain-explorer \
	-p 80:80 \
	-v $PWD:/usr/src/app \
	-e MAIN_APP_FILE=domainexplorer.rb \
	-d ruby-whois